# 功能

1、用户按照产品组合进行输入：图片 + 价格 + 感受。每个产品是一组卡片，每期帖子根据情况可以增减卡片数量

2、系统按产品卡片为单位手机本次所有输入，全部输出给后端。通过Agent系统返回一个可直接发送的帖子，包括：图片 标题  文案  标签。其中AI应该对产品图片进行风格化处理

3、后台自动将本次帖子打包成一次draft。用户需要手动点击已发送来修改这次项目的状态。状态包括：draft草稿、published已发表、analyzed已分析

4、系统在帖子发布后开始计时，一周后提醒用户进入该帖子并输入分析数据，分析数据包括： 浏览量 点赞数 收藏数 评论数 涨粉数。系统提供数据分析结果





# 技术栈

```
前端：React + Vite + TypeScript + Ant Design
后端：FastAPI
数据库：PostgreSQL
ORM：SQLAlchemy 2.0
任务调度：APScheduler
图片存储：阿里云OSS服务
LLM：OpenAI Responses API
部署：Docker Compose
```

理由很简单：你这个项目是 **内容工作流 + 图片上传 + AI生成 + 数据分析 + 定时提醒**，FastAPI 比 Django 更轻，React 更适合做运营后台，PostgreSQL 适合存结构化运营数据，MinIO 可以模拟真实 OSS，以后也方便迁移到阿里云 OSS。

OpenAI API 支持结构化输出，可以让 Agent 直接返回符合 JSON Schema 的内容，适合你的“标题、正文、标签、评分、复盘结果”等结构化生成场景；也支持图片输入，适合后续做图片理解。

# 系统整体架构

```
React Web 管理后台
  ↓
FastAPI 后端服务
  ↓
业务模块
  - 产品录入模块
  - 图片上传模块
  - 笔记生成 Agent
  - 数据录入模块
  - 数据分析 Agent
  - 提醒任务模块
  ↓
PostgreSQL
  - 产品表
  - 图片表
  - 帖子表
  - 指标表
  - 任务表
  ↓
OSS
  - 原始产品图片
  - 封面图片
  - 评测素材图
  ↓
OpenAI API
  - 图片分析
  - 文案生成
  - 数据复盘
```

# MVP 开发顺序

## 第一阶段：先做“帖子生产系统”

目标：你能上传产品信息，然后 Agent 生成一篇完整小红书笔记。

需要做：

```
1. 产品创建页面
2. 图片上传
3. 填写产品名称、价格、直观感受
4. 点击“生成笔记”
5. 后端调用 Agent
6. 返回标题、正文、标签、封面文案、图片
7. 存为一篇 draft 帖子
```

这是整个系统的核心闭环。

------

## 第二阶段：做“发布记录和数据录入”

目标：每篇帖子发布后，可以记录表现数据。

需要做：

```
1. 帖子列表
2. 帖子状态：draft / scheduled / published / analyzed
3. 发布日期
4. 一周后提醒录入数据
5. 数据录入表单：
   - 浏览量
   - 点赞数
   - 收藏数
   - 评论数
   - 涨粉数
6. 自动计算：
   - 互动率
   - 收藏率
   - 转粉率
```

------

## 第三阶段：做“复盘分析 Agent”

目标：让 Agent 根据历史帖子告诉你什么内容更容易起号。

分析维度：

```
标题风格
产品类别
价格区间
封面文案
正文结构
避雷 / 种草倾向
互动率
收藏率
转粉率
```

输出：

```
本周表现总结
表现最好的帖子
表现最差的帖子
可能原因
下一期选题建议
标题优化建议
封面优化建议
```

# 数据库设计

## products 产品卡片表

```
CREATE TABLE products (
  id UUID PRIMARY KEY,
  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
  sort_order INTEGER NOT NULL DEFAULT 0,
  product_name VARCHAR(255) NOT NULL,
  price NUMERIC(10,2),
  user_impression TEXT NOT NULL,

  agent_summary TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

## card_images 图片表

```
CREATE TABLE card_images (
  id UUID PRIMARY KEY,

  card_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,

  source_image_id UUID REFERENCES card_images(id) ON DELETE SET NULL,

  image_type VARCHAR(50) NOT NULL DEFAULT 'original',

  object_key TEXT NOT NULL,
  image_url TEXT NOT NULL,

  sort_order INTEGER NOT NULL DEFAULT 0,
  final_sort_order INTEGER,

  is_selected_for_post BOOLEAN NOT NULL DEFAULT FALSE,

  ai_description TEXT,

  width INTEGER,
  height INTEGER,
  file_size INTEGER,
  mime_type VARCHAR(100),

  is_deleted BOOLEAN NOT NULL DEFAULT FALSE,
  deleted_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

## image_processing_jobs图像处理表

```sql
CREATE TABLE image_processing_jobs (
  id UUID PRIMARY KEY,

  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
  card_id UUID REFERENCES products(id) ON DELETE CASCADE,
  source_image_id UUID NOT NULL REFERENCES card_images(id) ON DELETE CASCADE,
  result_image_id UUID REFERENCES card_images(id) ON DELETE SET NULL,

  style_preset_id UUID REFERENCES agent_style_presets(id) ON DELETE SET NULL,
  custom_prompt TEXT,
  processing_params JSONB,

  status VARCHAR(50) NOT NULL DEFAULT 'pending',		#pending / processing / completed / failed

  error_message TEXT,

  started_at TIMESTAMP,
  completed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```



## posts 帖子表 ：

```SQL
CREATE TABLE posts (
  id UUID PRIMARY KEY,

  status VARCHAR(50) NOT NULL DEFAULT 'draft',

  selected_title TEXT,
  title_options JSONB,
  content TEXT,
  cover_text TEXT,
  tags JSONB,

  image_style_preset_id UUID REFERENCES agent_style_presets(id) ON DELETE SET NULL,
  image_custom_prompt TEXT,
  image_custom_params JSONB,

  copy_style_preset_id UUID REFERENCES agent_style_presets(id) ON DELETE SET NULL,
  copy_custom_prompt TEXT,
  copy_custom_params JSONB,

  published_at TIMESTAMP,
  analyzed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

## post_metrics 数据表

```
CREATE TABLE post_metrics (
  id UUID PRIMARY KEY,

  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,

  views INTEGER NOT NULL DEFAULT 0,
  likes INTEGER NOT NULL DEFAULT 0,
  collects INTEGER NOT NULL DEFAULT 0,
  comments INTEGER NOT NULL DEFAULT 0,
  followers_gained INTEGER NOT NULL DEFAULT 0,

  interaction_rate NUMERIC(10,4),
  collect_rate NUMERIC(10,4),
  follower_rate NUMERIC(10,4),

  analysis_result TEXT,
  analysis_json JSONB,

  recorded_at TIMESTAMP DEFAULT NOW()
);
```

## reminders 提醒表

```
CREATE TABLE reminders (
  id UUID PRIMARY KEY,

  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,

  remind_type VARCHAR(50) NOT NULL DEFAULT 'metrics_input',
  remind_at TIMESTAMP NOT NULL,

  status VARCHAR(50) NOT NULL DEFAULT 'pending',

  message TEXT,

  created_at TIMESTAMP DEFAULT NOW(),
  completed_at TIMESTAMP
);
```



## agent_style_presets风格配置表

```
CREATE TABLE agent_style_presets (
  id UUID PRIMARY KEY,

  preset_type VARCHAR(50) NOT NULL,
  name VARCHAR(100) NOT NULL,
  description TEXT,

  prompt_template TEXT NOT NULL,
  default_params JSONB,

  is_system_default BOOLEAN NOT NULL DEFAULT FALSE,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,

  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```





# 后端接口设计

## 风格配置接口

```
GET    /api/style-presets
GET    /api/style-presets/defaults
POST   /api/style-presets
GET    /api/style-presets/{preset_id}
PATCH  /api/style-presets/{preset_id}
DELETE /api/style-presets/{preset_id}
```



## 帖子接口

```
POST   /api/posts
GET    /api/posts
GET    /api/posts/{post_id}
PATCH  /api/posts/{post_id}
DELETE /api/posts/{post_id}

POST   /api/posts/{post_id}/mark-published
POST   /api/posts/{post_id}/mark-analyzed
```

## 产品卡片接口

```
POST   /api/posts/{post_id}/products
GET    /api/posts/{post_id}/products
GET    /api/products/{product_id}
PATCH  /api/products/{product_id}
DELETE /api/products/{product_id}
POST   /api/posts/{post_id}/products/reorder
```

## 图片接口

```
POST   /api/products/{product_id}/images
GET    /api/products/{product_id}/images
GET    /api/images/{image_id}
PATCH  /api/images/{image_id}
DELETE /api/images/{image_id}
POST /api/products/{product_id}/images/reorder
POST /api/posts/{post_id}/images/select
```

## 图像处理 Agent 接口

```
POST   /api/images/{image_id}/process
POST /api/products/{product_id}/images/process
POST /api/posts/{post_id}/images/process
GET /api/image-processing-jobs/{job_id}
GET /api/posts/{post_id}/image-processing-jobs
GET /api/products/{product_id}/image-processing-jobs
POST   /api/image-processing-jobs/{job_id}/retry
POST /api/image-processing-jobs/{job_id}/cancel
```

## 文案生成 Agent 接口

```
POST   /api/posts/{post_id}/generate-copy
POST   /api/posts/{post_id}/regenerate-copy
```

## 帖子状态接口

```
POST /api/posts/{post_id}/mark-published
POST /api/posts/{post_id}/mark-analyzed
```



## 数据与分析接口

```
POST   /api/posts/{post_id}/metrics
GET    /api/posts/{post_id}/metrics
POST   /api/posts/{post_id}/analyze
GET    /api/posts/{post_id}/analysis
GET    /api/analytics/overview
```

## 提醒接口

```
GET    /api/reminders
GET    /api/reminders/pending
PATCH  /api/reminders/{reminder_id}
POST   /api/reminders/{reminder_id}/done
POST   /api/reminders/{reminder_id}/ignore
```

# Agent 设计

核心目标是：

```
用户输入：
每个产品 = 图片 + 价格 + 简单感受

Agent 输出：
1. 可直接发小红书的帖子文案
2. 风格化处理后的产品图片
3. 图片排序建议
4. 封面图建议
5. 结构化 draft 数据，方便写入数据库
```

## Agent 总体结构

4 个 Agent：

```
PostGenerationAgent 总编排 Agent
  ↓
ImageUnderstandingAgent 图片理解 Agent
  ↓
ImageStyleAgent 图片风格化处理 Agent
  ↓
CopywritingAgent 文案生成 Agent
```

整体流程：

```
用户创建帖子 draft
  ↓
输入多个产品卡片：图片 + 价格 + 感受
  ↓
ImageUnderstandingAgent 分析原图
  ↓
ImageStyleAgent 生成风格化处理图
  ↓
CopywritingAgent 生成标题、正文、标签、封面文案
  ↓
PostGenerationAgent 汇总结果
  ↓
写入 posts / products / card_images / image_processing_jobs
```

## 总 Agent：PostGenerationAgent

`PostGenerationAgent` 是总控，不直接写文案、不直接处理图片，而是负责任务编排。

职责：

```
1. 接收本期帖子的全部产品卡片输入
2. 读取用户选择的图片处理风格和文案生成风格
3. 调用 ImageUnderstandingAgent 分析图片
4. 调用 ImageStyleAgent 处理图片
5. 调用 CopywritingAgent 生成小红书文案
6. 合并所有 Agent 输出
7. 返回完整 draft 结构
8. 后端根据返回结果写入数据库
```

**输入格式**

```
{
  "post_id": "post-uuid",
  "image_style": {
    "style_preset_id": "image-style-uuid",
    "style_name": "干净测评风",
    "custom_prompt": "图片要更适合小红书，保持商品真实，不要改变包装文字",
    "params": {
      "aspect_ratio": "3:4",
      "quality": "high",
      "keep_product_original": true,
      "add_text_space": true,
      "background": "clean"
    }
  },
  "copy_style": {
    "style_preset_id": "copy-style-uuid",
    "style_name": "真实吐槽测评风",
    "custom_prompt": "文案要像普通用户真实体验，不要广告腔",
    "params": {
      "title_count": 5,
      "tone": "真实直观",
      "include_pros_cons": true,
      "include_suitable_people": true,
      "max_length": 600
    }
  },
  "products": [
    {
      "product_id": "product-uuid-1",
      "sort_order": 1,
      "product_name": "",
      "price": 6.9,
      "user_impression": "",
      "images": [
        {
          "image_id": "image-uuid-1",
          "image_url": "http://localhost:9000/xhs/original/1.jpg",
          "object_key": "xhs/original/1.jpg",
          "sort_order": 1
        },
        {
          "image_id": "image-uuid-2",
          "image_url": "http://localhost:9000/xhs/original/2.jpg",
          "object_key": "xhs/original/2.jpg",
          "sort_order": 2
        }
      ]
    }
  ]
}
```

**输出格式**

```
{
  "post_id": "post-uuid",
  "status": "draft",
  "copy_result": {
    "selected_title": "这几款Byt小零食，真的有必要买吗？",
    "title_options": [
      "这几款Byt小零食，真的有必要买吗？",
      "Byt零食真实测评：有惊喜，也有一点小踩雷",
      "不是广告！这期Byt测评我说点真实感受",
      "这几款Byt产品，适合办公室囤吗？",
      "吃完这几款Byt，我的真实感受是……"
    ],
    "content": "完整正文内容",
    "tags": ["Byt测评", "小红书测评", "真实测评", "零食测评"],
    "comment_guide": "你们还想看哪款Byt产品测评？可以评论区告诉我。",
    "recommendation_level": "中立偏种草"
  },
  "product_results": [
    {
      "product_id": "product-uuid-1",
      "product_name": "某某气泡水",
      "agent_summary": "包装颜值不错，口感清爽，但甜味偏明显。",
      "agent_recommendation": "轻种草",
      "keywords": ["包装好看", "口感清爽", "甜味明显"],
      "suitable_for": "喜欢清爽甜口饮品的人",
      "not_suitable_for": "不喜欢甜味明显饮料的人"
    }
  ],
  "image_results": [
    {
      "source_image_id": "image-uuid-1",
      "result_image_id": "processed-image-uuid-1",
      "image_type": "processed",
      "image_url": "http://localhost:9000/xhs/processed/1.jpg",
      "ai_description": "图片中产品主体清晰，包装颜色明亮，适合作为封面候选。",
      "visual_keywords": ["主体清晰", "颜色明亮", "适合封面"],
      "is_cover_candidate": true,
      "recommended_final_sort_order": 1
    }
  ],
  "final_image_plan": {
    "cover_image_id": "processed-image-uuid-1",
    "selected_image_ids": [
      "processed-image-uuid-1",
      "processed-image-uuid-2"
    ],
    "image_order_suggestion": "建议第一张使用主体最清晰的产品图作为封面，后续按产品出现顺序排列。"
  }
}
```

## 1 号子 Agent：ImageUnderstandingAgent

这个 Agent 负责“看懂原图”，不负责生成图片。

1. **职责**

```
1. 分析产品图片中有什么
2. 判断图片是否清晰
3. 判断是否适合作为封面
4. 提取视觉关键词
5. 给图片处理 Agent 提供图像处理建议
6. 给文案 Agent 提供视觉描述
```

**2. 输入格式**

```
{
  "post_id": "post-uuid",
  "product": {
    "product_id": "product-uuid-1",
    "product_name": "某某气泡水",
    "price": 6.9,
    "user_impression": "包装好看，口感清爽，但是甜味有点明显"
  },
  "image": {
    "image_id": "image-uuid-1",
    "image_url": "http://localhost:9000/xhs/original/1.jpg",
    "object_key": "xhs/original/1.jpg",
    "sort_order": 1
  }
}
```

**3. 输出格式**

```
{
  "image_id": "image-uuid-1",
  "product_id": "product-uuid-1",
  "image_summary": "图片中是一瓶气泡水，产品主体清晰，包装颜色偏清爽，背景较干净。",
  "main_subject": "气泡水瓶身",
  "background_description": "背景较干净，但有少量桌面杂物",
  "visual_keywords": ["主体清晰", "包装清爽", "适合测评图"],
  "quality_check": {
    "is_clear": true,
    "is_product_visible": true,
    "has_bad_lighting": false,
    "has_cluttered_background": false,
    "has_text_on_package": true
  },
  "cover_candidate": true,
  "cover_candidate_reason": "产品主体完整，包装清晰，画面简单，适合作为封面。",
  "style_suggestion": {
    "should_process": true,
    "suggested_crop": "3:4",
    "keep_package_text": true,
    "background_adjustment": "clean",
    "lighting_adjustment": "soft"
  }
}
```

**4. 写入数据库位置**

对应写入：

```
card_images.ai_description
```

## 2 号子 Agent：ImageStyleAgent

这个 Agent 负责“生成适合小红书浏览的风格化产品图”。

**1. 职责**

```
1. 接收原图
2. 接收图片理解结果
3. 接收系统默认图片风格或用户自定义风格
4. 生成处理后的图片
5. 保持产品主体真实
6. 尽量不改变产品包装、Logo、文字和形状
7. 输出处理后的图片 URL 和处理任务信息
```

**2. 输入格式**

```
{
  "job_id": "job-uuid",
  "post_id": "post-uuid",
  "product_id": "product-uuid-1",
  "source_image": {
    "image_id": "image-uuid-1",
    "image_url": "http://localhost:9000/xhs/original/1.jpg",
    "object_key": "xhs/original/1.jpg"
  },
  "image_understanding": {
    "image_summary": "图片中是一瓶气泡水，产品主体清晰，包装颜色偏清爽，背景较干净。",
    "main_subject": "气泡水瓶身",
    "quality_check": {
      "is_clear": true,
      "is_product_visible": true,
      "has_text_on_package": true
    },
    "style_suggestion": {
      "suggested_crop": "3:4",
      "keep_package_text": true,
      "background_adjustment": "clean",
      "lighting_adjustment": "soft"
    }
  },
  "style_config": {
    "style_preset_id": "image-style-uuid",
    "style_name": "干净测评风",
    "custom_prompt": "图片要适合小红书浏览，背景干净，主体突出，但不要改变商品包装文字。",
    "params": {
      "aspect_ratio": "3:4",
      "quality": "high",
      "background": "clean",
      "keep_product_original": true,
      "add_text_space": true
    }
  }
}
```

**3. 输出格式**

```
{
  "job_id": "job-uuid",
  "status": "completed",
  "source_image_id": "image-uuid-1",
  "result_image": {
    "image_id": "processed-image-uuid-1",
    "image_type": "processed",
    "object_key": "xhs/processed/processed-image-uuid-1.png",
    "image_url": "http://localhost:9000/xhs/processed/processed-image-uuid-1.png",
    "width": 1080,
    "height": 1440,
    "mime_type": "image/png",
    "file_size": 523000
  },
  "style_applied": {
    "style_preset_id": "image-style-uuid",
    "style_name": "干净测评风",
    "params": {
      "aspect_ratio": "3:4",
      "quality": "high",
      "background": "clean",
      "keep_product_original": true
    }
  },
  "post_usage_suggestion": {
    "is_cover_candidate": true,
    "recommended_final_sort_order": 1,
    "reason": "主体清晰、画面干净，适合作为封面图。"
  },
  "error_message": null
}
```

**4. 失败输出格式**

```
{
  "job_id": "job-uuid",
  "status": "failed",
  "source_image_id": "image-uuid-1",
  "result_image": null,
  "error_message": "图片处理失败，原因是原图分辨率过低或主体不完整。"
}
```

**5. 写入数据库位置**

处理任务写入：

```
image_processing_jobs
```

处理后的图片写入：

```
card_images
```

字段对应：

```
card_images.source_image_id = 原图 id
card_images.image_type = processed
card_images.object_key = 处理图 object_key
card_images.image_url = 处理图 URL
image_processing_jobs.result_image_id = 处理图 id
image_processing_jobs.status = completed
```

## 3 号子 Agent：CopywritingAgent

这个 Agent 负责生成小红书帖子的完整文案。

**1. 职责**

```
1. 根据所有产品卡片生成整篇帖子文案
2. 保留用户真实感受
3. 不编造用户没有提供的体验
4. 生成 3-5 个标题
5. 生成正文
6. 生成封面文案
7. 生成标签
8. 生成评论区引导语
9. 判断整体种草/避雷程度
10. 给每个产品生成简短总结
```

**2. 输入格式**

```
{
  "post_id": "post-uuid",
  "copy_style": {
    "style_preset_id": "copy-style-uuid",
    "style_name": "真实吐槽测评风",
    "custom_prompt": "文案要像真实用户分享，不要广告腔，有一点轻微吐槽感。",
    "params": {
      "title_count": 5,
      "tone": "真实直观",
      "max_length": 600,
      "include_pros_cons": true,
      "include_suitable_people": true
    }
  },
  "products": [
    {
      "product_id": "product-uuid-1",
      "sort_order": 1,
      "product_name": "某某气泡水",
      "price": 6.9,
      "user_impression": "包装好看，口感清爽，但是甜味有点明显",
      "image_summaries": [
        {
          "image_id": "image-uuid-1",
          "image_summary": "产品主体清晰，包装颜色清爽，适合做封面。",
          "visual_keywords": ["主体清晰", "包装清爽"]
        }
      ]
    },
    {
      "product_id": "product-uuid-2",
      "sort_order": 2,
      "product_name": "某某饼干",
      "price": 9.9,
      "user_impression": "入口很香，但后面有点腻，性价比一般",
      "image_summaries": [
        {
          "image_id": "image-uuid-3",
          "image_summary": "包装精致，适合放在正文第二张展示。",
          "visual_keywords": ["包装精致", "适合展示"]
        }
      ]
    }
  ],
  "selected_images": [
    {
      "image_id": "processed-image-uuid-1",
      "product_id": "product-uuid-1",
      "is_cover_candidate": true,
      "recommended_final_sort_order": 1
    }
  ]
}
```

**3. 输出格式**

```
{
  "post_id": "post-uuid",
  "selected_title": "Byt这几款小东西，我真实测完有点意外",
  "title_options": [
    "Byt这几款小东西，我真实测完有点意外",
    "这期Byt测评：有惊喜，也有一点小踩雷",
    "不是广告！Byt这几款我说点真实感受",
    "Byt产品真实测评，哪些值得买？",
    "这几款Byt到底是不是智商税？"
  ],
  "content": "这期测了几款Byt产品，整体感受是：不是完全踩雷，但也不是每个都值得无脑冲。\n\n第一个是某某气泡水，价格 6.9 元。包装确实挺好看，口感也比较清爽，但甜味会比我预期明显一点。如果你喜欢甜口，应该会觉得不错；但如果你平时喝无糖饮料多，可能会觉得负担有点重。\n\n第二个是某某饼干，入口很香，但吃到后面会有点腻。性价比我觉得一般，适合尝鲜，不太适合囤很多。\n\n我的结论是：可以试，但不用盲目囤。更适合喜欢尝新品、对包装颜值有要求的人。",
  "cover_text": "Byt真实测评\n有惊喜也有小踩雷",
  "tags": [
    "Byt测评",
    "真实测评",
    "小红书测评",
    "零食测评",
    "避雷种草"
  ],
  "comment_guide": "你们还想看哪款Byt产品？评论区告诉我，下期可以继续测。",
  "recommendation_level": "中立偏种草",
  "product_copy_results": [
    {
      "product_id": "product-uuid-1",
      "agent_summary": "包装颜值高，口感清爽，但甜味明显。",
      "agent_recommendation": "轻种草",
      "pros": ["包装好看", "口感清爽"],
      "cons": ["甜味偏明显"],
      "suitable_for": "喜欢甜口、重视包装颜值的人",
      "not_suitable_for": "偏爱无糖或低甜饮料的人"
    },
    {
      "product_id": "product-uuid-2",
      "agent_summary": "入口香，但后段略腻，性价比一般。",
      "agent_recommendation": "中立",
      "pros": ["入口香", "适合尝鲜"],
      "cons": ["容易腻", "性价比一般"],
      "suitable_for": "想尝新品的人",
      "not_suitable_for": "追求高性价比或不喜欢甜腻口感的人"
    }
  ],
  "image_order_suggestion": "建议第一张放主体最清晰、视觉最干净的处理图作为封面；后续图片按产品测评顺序排列，保证用户阅读顺序和正文一致。"
}
```

**4. 写入数据库位置**

写入 `posts`：

```
selected_title
title_options
content
cover_text
tags
```

写入 `products`：

```
agent_summary
agent_recommendation
```






