# 功能

1、用户按照产品组合进行输入：图片 + 价格 + 感受。每个产品是一组卡片，每期帖子根据情况可以增减卡片数量

2、系统按产品卡片为单位手机本次所有输入，全部输出给后端。通过Agent系统返回一个可直接发送的帖子，包括：图片 标题  文案  标签。其中AI应该对产品图片进行风格化处理

3、后台自动将本次帖子打包成一次draft。用户需要手动点击已发送来修改这次项目的状态。状态包括：draft草稿、published已发表、analyzed已分析

4、系统在帖子发布后开始计时，一周后提醒用户进入该帖子并输入分析数据，分析数据包括： 浏览量 点赞数 收藏数 评论数 涨粉数。系统提供数据分析结果





# 技术栈

```
前端：React + Vite + TypeScript + Ant Design
后端：FastAPI
数据库：PostgreSQL
ORM：SQLAlchemy 2.0
任务调度：APScheduler
图片存储：阿里云OSS服务
LLM：OpenAI Responses API
部署：Docker Compose
```

理由很简单：你这个项目是 **内容工作流 + 图片上传 + AI生成 + 数据分析 + 定时提醒**，FastAPI 比 Django 更轻，React 更适合做运营后台，PostgreSQL 适合存结构化运营数据，MinIO 可以模拟真实 OSS，以后也方便迁移到阿里云 OSS。

OpenAI API 支持结构化输出，可以让 Agent 直接返回符合 JSON Schema 的内容，适合你的“标题、正文、标签、评分、复盘结果”等结构化生成场景；也支持图片输入，适合后续做图片理解。

# 系统整体架构

```
React Web 管理后台
  ↓
FastAPI 后端服务
  ↓
业务模块
  - 产品录入模块
  - 图片上传模块
  - 笔记生成 Agent
  - 数据录入模块
  - 数据分析 Agent
  - 提醒任务模块
  ↓
PostgreSQL
  - 产品表
  - 图片表
  - 帖子表
  - 指标表
  - 任务表
  ↓
OSS
  - 原始产品图片
  - 封面图片
  - 评测素材图
  ↓
OpenAI API
  - 图片分析
  - 文案生成
  - 数据复盘
```

# MVP 开发顺序

## 第一阶段：先做“帖子生产系统”

目标：你能上传产品信息，然后 Agent 生成一篇完整小红书笔记。

需要做：

```
1. 产品创建页面
2. 图片上传
3. 填写产品名称、价格、直观感受
4. 点击“生成笔记”
5. 后端调用 Agent
6. 返回标题、正文、标签、封面文案、图片
7. 存为一篇 draft 帖子
```

这是整个系统的核心闭环。

------

## 第二阶段：做“发布记录和数据录入”

目标：每篇帖子发布后，可以记录表现数据。

需要做：

```
1. 帖子列表
2. 帖子状态：draft / scheduled / published / analyzed
3. 发布日期
4. 一周后提醒录入数据
5. 数据录入表单：
   - 浏览量
   - 点赞数
   - 收藏数
   - 评论数
   - 涨粉数
6. 自动计算：
   - 互动率
   - 收藏率
   - 转粉率
```

------

## 第三阶段：做“复盘分析 Agent”

目标：让 Agent 根据历史帖子告诉你什么内容更容易起号。

分析维度：

```
标题风格
产品类别
价格区间
封面文案
正文结构
避雷 / 种草倾向
互动率
收藏率
转粉率
```

输出：

```
本周表现总结
表现最好的帖子
表现最差的帖子
可能原因
下一期选题建议
标题优化建议
封面优化建议
```

# 数据库设计

## products 产品卡片表

```
CREATE TABLE products (
  id UUID PRIMARY KEY,
  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
  sort_order INTEGER NOT NULL DEFAULT 0,
  product_name VARCHAR(255) NOT NULL,
  price NUMERIC(10,2),
  user_impression TEXT NOT NULL,

  agent_summary TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

## product_images 图片表

```
CREATE TABLE card_images (
  id UUID PRIMARY KEY,
  card_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,
  object_key TEXT NOT NULL,
  image_url TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  ai_description TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

## posts 帖子表 ：

```SQL
CREATE TABLE posts (
  id UUID PRIMARY KEY,

  status VARCHAR(50) NOT NULL DEFAULT 'draft',  #draft/published/analyzed

  selected_title TEXT,				# 最终选中的标题
  title_options JSONB,				# Agent生成的多个标题候选
  content TEXT,						# 最终正文文案
  cover_text TEXT,					# 待定
  tags JSONB,						# 小红书标签
  published_at TIMESTAMP,			# 用户点击发布的时间
  analyzed_at TIMESTAMP,			# 用户完成数据录入并分析的时间
  created_at TIMESTAMP DEFAULT NOW(),	# 草稿创建时间
  updated_at TIMESTAMP DEFAULT NOW()	# 最后修改时间
);
```

## post_metrics 数据表

```
CREATE TABLE post_metrics (
  id UUID PRIMARY KEY,

  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,

  views INTEGER NOT NULL DEFAULT 0,
  likes INTEGER NOT NULL DEFAULT 0,
  collects INTEGER NOT NULL DEFAULT 0,
  comments INTEGER NOT NULL DEFAULT 0,
  followers_gained INTEGER NOT NULL DEFAULT 0,

  interaction_rate NUMERIC(10,4),
  collect_rate NUMERIC(10,4),
  follower_rate NUMERIC(10,4),

  analysis_result TEXT,
  analysis_json JSONB,

  recorded_at TIMESTAMP DEFAULT NOW()
);
```

## reminders 提醒表

```
CREATE TABLE reminders (
  id UUID PRIMARY KEY,

  post_id UUID NOT NULL REFERENCES posts(id) ON DELETE CASCADE,

  remind_type VARCHAR(50) NOT NULL DEFAULT 'metrics_input',
  remind_at TIMESTAMP NOT NULL,

  status VARCHAR(50) NOT NULL DEFAULT 'pending',

  message TEXT,

  created_at TIMESTAMP DEFAULT NOW(),
  completed_at TIMESTAMP
);
```

# 后端接口设计

## 产品相关

```
POST   /api/products
GET    /api/products
GET    /api/products/{id}
POST   /api/products/{id}/images
```

## Agent 生成相关

```
POST   /api/posts/generate
GET    /api/posts/{id}
PATCH  /api/posts/{id}
POST   /api/posts/{id}/publish
```

## 数据相关

```
POST   /api/posts/{id}/metrics
GET    /api/posts/{id}/metrics
GET    /api/analytics/weekly
POST   /api/analytics/review
```

## 提醒相关

```
GET    /api/reminders
PATCH  /api/reminders/{id}/done
```

# Agent 设计

## 总 Agent：PostGenerationAgent

职责：

```
接收产品信息
读取产品图片分析结果
调用图片处理 Agent
调用文案生成 Agent
合并成完整帖子
输出结构化 JSON
存入 posts 表
```

------

## 1 号子 Agent：ImageAgent

职责：

```
分析图片内容
判断图片适合做封面还是正文
给出图片排序建议
提取包装、颜色、质感、卖点信息
```

输出结构：

```
{
  "image_summary": "图片整体描述",
  "cover_candidate": true,
  "visual_keywords": ["包装精致", "颜色清爽", "适合封面"],
  "image_order_reason": "第一张适合做封面，因为主体清晰"
}
```

------

## 2 号子 Agent：CopywritingAgent

职责：

```
生成 3-5 个标题
生成正文
生成封面文案
生成标签
生成评论区引导语
判断避雷/种草程度
```

输出结构：

```
{
  "title_options": [],
  "content": "",
  "cover_text": "",
  "image_order_suggestion": "",
  "tags": [],
  "comment_guide": "",
  "recommendation_level": "轻种草 / 中立 / 避雷"
}
```

# 前端页面设计

## 页面 1：首页 Dashboard

展示：

```
本周待发布数量
待录入数据帖子
已发布帖子数量
平均互动率
平均收藏率
表现最好的帖子
下一期提醒
```

------

## 页面 2：新建测评

表单字段：

```
产品名称
产品分类
价格
购买渠道
直观感受
适合人群
不适合人群
上传图片
生成笔记按钮
```

------

## 页面 3：帖子草稿详情

展示：

```
标题候选
正文
封面文案
标签
图片排序建议
评论区引导语
避雷/种草程度
```

操作：

```
编辑
保存
标记为已发布
设置发布时间
```

------

## 页面 4：数据录入

字段：

```
浏览量
点赞数
收藏数
评论数
涨粉数
```

自动展示：

```
互动率
收藏率
转粉率
```

------

## 页面 5：复盘分析

展示：

```
本周数据总结
最佳帖子
最差帖子
标题表现分析
封面表现分析
下一期建议
```